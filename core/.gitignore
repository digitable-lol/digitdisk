# Собранное из напечатанного. Сам напечатанный Go и C коммитятся (AGENTS.md,
# «core/out-go печатается, а не пишется»), их сборка — нет: бинарь молча
# разойдётся с исходником, а восстанавливается он одной командой
# `make -C core печать`.
out-go/flang_cli
out-c/flang_cli
out-c/*.o
out-c/*.a
